# Midea / Senville S-Comms Monitor - Dependencies
#
# It is recommended to run this project inside a virtual environment:
#
#   python3 -m venv venv
#   source venv/bin/activate        # Linux / macOS
#   venv\Scripts\activate           # Windows
#   pip install -r requirements.txt
#
# To deactivate the venv when done:
#   deactivate

# MQTT client for Home Assistant integration
paho-mqtt>=1.6.1

# SQLite is included in Python's standard library (no install needed)
# asyncio is included in Python's standard library (no install needed)
